# dev-docs-skill 运行时依赖
# ----------------------------------------
# 所有脚本只使用 Python 3.8+ 标准库，零外部依赖。
# 直接 `python scripts/<script>.py` 即可运行。
#
# Python 版本要求：>= 3.8（使用了 dataclasses、typing.Optional[Callable] 等）
#
# ----------------------------------------
# 可选：开发期工具（运行脚本本身不需要）
# ----------------------------------------
# black>=23.0.0       # 代码格式化
# ruff>=0.1.0         # 代码静态检查（推荐替代 flake8）
# mypy>=1.0.0         # 类型检查
# pytest>=7.0.0       # 单元测试
#
# ----------------------------------------
# 可选：generate_api_doc.py 的 OpenAPI YAML 解析
# ----------------------------------------
# generate_api_doc.py 解析 OpenAPI 文件时：
#   - .json  → 使用标准库 json 模块（无需依赖）
#   - .yaml / .yml → 需要 PyYAML
#
# 如果你只用 OpenAPI JSON 或仅做源码扫描，可不安装。
# PyYAML>=6.0
